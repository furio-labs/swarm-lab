---
description: Re-run the standing skeptic across all current business-case artifacts and produce a bear case.
---
Invoke `red-team-partner` to audit every `docs/business-model/ws*.md` that exists. For each,
refresh or write its `.challenge.md`. Then produce a consolidated `docs/business-model/BEAR-CASE.md`:
the single strongest memo arguing AGAINST funding this company, drawing on the cross-artifact
contradictions and any un-rebutted kill-shots (start from `candidate_risks` in 00-CONTEXT). If the
team can't answer the bear case, say so.
